<InputText @bind-Value="value" hidden="hidden" />

@code {
    [Parameter] public Expression<Func<object>> For { get; set; }

    private string _;

    private string value
    {
        get => For?.ToString() ?? String.Empty;
        set => _ = value;
    }
}